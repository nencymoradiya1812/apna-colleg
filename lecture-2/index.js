let pop=document.querySelector("button")
pop.addEventListener("click",input)
function input(){
                  let  name =prompt("enter name of student");
                  name.tectcontent="roll no.1" + name;
}